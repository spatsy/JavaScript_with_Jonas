//we just added the script tag in this demo I also played aroung with the body tag and changed the color and gradient.if we placed this in the head tag, this would be an inline script but it's generally frowned upon
let js = 'amazing';
if (js === 'amazing') alert('JavaScript is FUN!')
//this is a console log and only shows up in the browser console and doesn't get printed on the page -->
console.log(40 + 8 + 23 - 11);
